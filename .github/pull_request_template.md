## What was wrong

<!-- The behaviour before this change. Not what the patch does; the code says that. -->

## How this fixes it

## Checklist

- [ ] Commits are signed off (`git commit -s`), per [DCO](../DCO)
- [ ] A test covers the change, and fails without it
- [ ] `npm run test:units` and `cd serve && go test ./...` pass
- [ ] No new dependencies
