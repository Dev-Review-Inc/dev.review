{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window may reach. Storage goes through this app's own commands, which need no permission, so nothing filesystem-shaped is granted here.",
  "windows": ["main"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default"
  ]
}
